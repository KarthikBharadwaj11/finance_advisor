## Frontend

React 18 + TypeScript + Vite frontend for FinanceAdv AI.

See the root [README](../README.md) for full project documentation and setup instructions.

### Dev server

```bash
npm install
npm run dev
```

Runs at `http://localhost:5173` and proxies `/api` to the backend at `localhost:8000`.
